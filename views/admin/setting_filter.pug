extends ./layout.pug

block pre-include
  - menu = 'product';
  - childMenu = 'filter';
  title Thuộc tính

block post-include
  script(src=staticURI() + '/js/admin/filter.js')

block content
  section.content-header.clearfix
    ol.breadcrumb.col-md-8.col-sm-7.hidden-xs
      li
        a Thuộc tính
      li
        a Danh sách thuộc tính
    ol.button-right.col-sm-4
      li
        a.btn.btn-admin(data-toggle="modal" data-target="#modal-add") Thêm thuộc tính
  section.content.filter.clearfix
    each item in data
      .col-lg-4.col-md-6.col-sm-12.col-xs-12
        .filter-card.clearfix(data-id=item.id)
          .title=item.title
            .group-btn
              i.fa.fa-edit.edit-filter.edit-attribute(data-id=item.id)
              i.fa.fa-trash.delete-filter(data-id=item.id)
          .option-list
            if item.child
              .clearfix
                .col-sm-4 Tên option
                .col-sm-4 Giá trị option
                .col-sm-4.text-right Chỉnh sửa
              each child in item.child
                if child.id
                  .clearfix
                    .col-sm-4=child.title
                    .col-sm-4=child.value
                    .col-sm-4.text-right
                      i.fa.fa-edit.edit-filter.edit-option(data-id=child.id)
                      i.fa.fa-trash.delete-filter(data-id=child.id, data-option=1)
          .col-xs-12.group-add-new-option
            .input-group
              input.form-control(type='text', name="title", placeholder="Tên option (required)")
              input.form-control(type='text', name="value", placeholder="Giá trị (optional)")
              .col-xs-12.text-center
                button.btn.btn-default.add-new-option(data-parent_id=item.id) Thêm option mới

    #modal-add.modal.fade(role='dialog')
      .modal-dialog
        .modal-content.form-add-filter
          .modal-header
            button.close(type='button', data-dismiss='modal') ×
            h4.modal-title Thêm
          .modal-body.clearfix
            .form-horizontal
              .box-body
                .form-group
                  label.col-sm-3.control-label Tên thuộc tính
                  .col-sm-9
                    input.form-control(type="text" name="title" placeholder="Màu sắc" required)
          .modal-footer
            button.btn.btn-default(type='button', data-dismiss='modal') Đóng
            button.btn.btn-admin.btn-create
              i.fa.fa-floppy-o
              i.fa.fa-circle-o-notch.fa-spin
              | Lưu lại
    #modal-edit.modal.fade(role='dialog')
      .modal-dialog
        .modal-content.form-add-filter
          .modal-header
            button.close(type='button', data-dismiss='modal') ×
            h4.modal-title Chỉnh sửa
          .modal-body.clearfix
            .form-horizontal
              .box-body
                .form-group
                  label.col-sm-3.control-label Tên thuộc tính
                  .col-sm-9
                    input.form-control(type="text" name="title" placeholder="Màu sắc" required)
          .modal-footer
            button.btn.btn-default(type='button', data-dismiss='modal') Đóng
            button.btn.btn-admin.btn-update
              i.fa.fa-floppy-o
              i.fa.fa-circle-o-notch.fa-spin
              | Cập nhật
    #modal-edit-option.modal.fade(role='dialog')
      .modal-dialog
        .modal-content.form-add-filter
          .modal-header
            button.close(type='button', data-dismiss='modal') ×
            h4.modal-title Chỉnh sửa
          .modal-body.clearfix
            .form-horizontal
              .box-body
                .form-group
                  label.col-sm-3.control-label Tên option
                  .col-sm-9
                    input.form-control(type="text" name="title" placeholder="Màu sắc" required)
                .form-group
                  label.col-sm-3.control-label Giá trị option
                  .col-sm-9
                    input.form-control(type="text" name="value" placeholder="Màu sắc" required)
          .modal-footer
            button.btn.btn-default(type='button', data-dismiss='modal') Đóng
            button.btn.btn-admin.btn-update
              i.fa.fa-floppy-o
              i.fa.fa-circle-o-notch.fa-spin
              | Cập nhật
